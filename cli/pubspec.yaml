name: cli
pubish_to: none
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/the-cozy-coder/dartpedia


environment:
  sdk: ^3.12.2

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  http: ^1.4.0
  command_runner: 
    path: ../command_runner

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
